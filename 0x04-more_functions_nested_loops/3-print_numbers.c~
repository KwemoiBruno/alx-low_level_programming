#include "main.h"
#include <stdio.h>

/**
 * print_numbers - prints numbers from 0 to 9
 * Return: always zero
 */
void print_numbers(void)
{
int i = '0';
while (i < '10')
{
printf("%d", i);
_putchar(i);
i++;
}
_putchar('\n');
}
