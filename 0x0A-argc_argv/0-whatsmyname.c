#include <stdio.h>

/**
 * main - prints its name
 * @argc: arguments count
 * @argv: arguments
 *
 * Return: nothing
 */
int main(int argc, char **argv)
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
